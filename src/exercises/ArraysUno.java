package exercises;

public class ArraysUno {
    public static void main(String[] args) {
        int[] someInts = {1, 1, 2, 3, 5, 8};
//        created an array
        for (int i = 0; i < someInts.length; i++) {
            if (someInts[i] % 2 != 0) {
                System.out.println(someInts[i]);
                //looping through array so every one prints
            }

        }
    }
    String suessSentence = ("I would not, could not, in a box. I would not, could not with a fox. I will not eat them in a house. I will not eat them with a mouse.");

}
